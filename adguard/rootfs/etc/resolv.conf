# Static (no DHCP anymore): resolve through the local AdGuardHome itself.
# Only affects libc/Go OS-resolver lookups inside the container; AdGuardHome
# resolves its DoT upstreams via its own bootstrap_dns from the yaml.
nameserver 127.0.0.1
