# Host-networked recovery member: it must come up while the estate's own
# resolvers (whole-house DNS) are down, so it depends on public resolvers
# instead of anything on this box. Only affects libc lookups inside the
# container; ZeroTier reaches its roots by literal IP anyway.
nameserver 9.9.9.9
nameserver 1.1.1.1
